<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alumno.proyecto_kebabs">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PantallaPresentacion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <!--
        ><uses-permission android:name="android.permission.CALL_PHONE">

        </uses-permission>
                -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PantallaUnoLogin"
            android:label="@string/app_name"></activity>

        <activity
            android:name=".PantallaDosMenuComida"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".PantallaTresMenuBebida"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".PantallaCuatroResumenPedido"
            android:label="@string/app_name"></activity>
    </application>

</manifest>